# HISTÓRICO — Era Chrome MCP do Dashboard de Compras (DESATIVADO 2026-06-01)

> ⚠️ NADA aqui deve ser executado. Chrome MCP é PROIBIDO para coleta do Microvix.
> Mantido apenas como referência da lógica de negócio implementada hoje em build_dashboard.py / coleta_compras.mjs.

**Histórico (Chrome MCP — DESATIVADO, mantido só como referência da lógica de negócio):**
- **Etapa 2/3 (saldo+vendas, ~60-90s em PARALELO):** rodar **4 chamadas per-loja em PARALELO** (4 tabs simultâneas), SEM filtro de marca, abrindo `relatorio_compra_venda_saldo_empresa.asp` em NOVA TAB (não iframe — iframe trava). Analítica + agrupado por Marca. Cada chamada retorna ~3000 produtos com `cells.length=11`. Salvar cada loja em `localStorage` (`__L1_FULL__`, etc.) — compartilhado entre tabs do mesmo origin. Mesclar pelos `codigo` no fim. **Ganho:** ~4 min sequencial → ~60-90s paralelo (limitado pela loja mais lenta). Plano futuro de REST direto (~30s) anotado no SKILL.
- **Atribuição de marca:** usa o cabeçalho de grupo `Marca: NOME (id)` do Analítica (NÃO match por nome). Aliases: Mirra→MIRRAS, Cinco→5 CINCO, Yama→YAMÁ, Apice→APSE. Marcas com múltiplos grupos no ERP: ProBelle [858+366]→"PROBELLE PROFISSIONAL"+"PROBELLE VAREJO", Igora→"IGORA ROYAL"+"IGORA" — somar todos.
- **Etapa 3.5:** Notas de Compra (`relatorio_notas.asp?modulo=estoque`) para `compras_mensais_rs` em R$. CFOP `[E]...COMPRA`. NOVA TAB obrigatória. ⚠️ **`tipo_data = data_documento`** (radio "Emissão", NÃO "Lançamento" — bug reportado 2026-05-14). Mês = data de **emissão da NFe**, não data de lançamento no ERP (que pode ser meses depois). ⚠️ **MATCH POR CÓDIGO ERP, NÃO POR DESCRIÇÃO** — montar `codigoToMarca = {p.codigo: mk.marca}` a partir de `dados.marcas[].produtos[]` (já populado pela Etapa 2/3 via filtro de marca) e dar lookup direto no código de cada item da nota. Match por keyword/descrição falha pra Natum/Yama/Marco Boni/Widi Care/Truss/Santa Clara (produtos sem nome da marca na descrição) e zera `compras_mensais_rs`. Bug reportado pelo usuário em 2026-05-14.
- **Detecção de marca em NFe pendente (Etapa 3.6) — DOIS passos:** (1) regex de keywords em `DescricaoProduto`; (2) se descrição não casou, consultar `fornecedor_marcas.json` por CNPJ (`DadosEmitente.Documento`) ou substring de `DadosEmitente.Nome`. Sem esse passo 2, fornecedores cujos produtos não trazem o nome da marca (Kamaleão Color é o exemplo padrão) ficam órfãos. Atual mapping: KAMALEAO COLOR (CNPJ 31768865000126) → Kamaleao.
- **FILTRO DE ANO CORRENTE (regra usuário 2026-05-15):** após receber resposta da API, **descartar NFes com `DataEmissao` de ano anterior** ao ano corrente, antes de entrarem em `compras_mensais_rs` ou trânsito. NFes pendentes podem ficar paradas no SEFAZ por meses sem entrar no ERP — uma emitida em ago/2025 ainda aparece pendente em mai/2026 e iria parar no mês 8 do dashboard 2026 (errado, é ano passado). Regra prática: `mes > mes_atual → necessariamente ano anterior → descartar`. Aplicar mesma regra na Etapa 3.5 (filtro `data_inicial = 01/01/<ano_corrente>`).
- **Etapa 3.6 (trânsito NFe per-PRODUTO, ~10s):** API direta `POST https://fiscalwebapi-prod.microvix.com.br/api/NfeEntrada/ObterListaNFesPendentesPorEmpresa` com body `{IdEmpresa, DataInicial:hoje-90d, DataFinal:hoje, Status:'Validos'}`. **JANELA DE 90 DIAS** (definida pelo usuário em 2026-05-15) — 60d perdia NFes antigas pendentes, 365d trazia ruído (livros digitais etc); 90d é o equilíbrio. Header `Authorization: <token>` (token de `localStorage.getItem('token_api')` da página `gestor_web/produtos/entrada_nfe/index.html`, dura ~24h). Resposta traz `NFes[].Produtos[]` com `CProd`, `CEAN`, `DescricaoProduto`, `QuantidadeComercial`. Mesmo token serve para as 4 empresas. Filtrar **excludente, não inclusivo**: excluir só `AMOSTRA|REMESSA EM CONSIGNAÇÃO|BONIFIC|DEVOLUÇÃO|RETORNO|TRANSFER` — incluir todo o resto (VENDA, COMPRA, OUTRAS ENTRADAS, etc). Matching produto NFe ↔ ERP em 3 níveis dentro da marca: (a) descrição exata; (b) `CProd`↔`referencia` (só dentro da marca já detectada — codes colidem entre fornecedores); (c) fuzzy ≥50% tokens. Não-matched dentro da marca = "órfão" somado no brand-level. Detectar marca de TODAS as marcas em `dados.json` (não só curva S). Logar no relatório final: `NFes pendentes API: L1=N L3=N L4=N L5=N` e `Aplicadas no dashboard: N de M`. ⚠️ CORS bloqueia chamada do parent — sempre rodar dentro de um documento da pasta `/gestor_web/`.
- **Etapa 3.7 (zerar trânsito de marca que já deu entrada no mês):** APÓS 3.5 e 3.6, ANTES de calcular sugestões. Para cada (marca × loja): se `compras_mensais_rs[loja][mes_corrente] > 0` E o valor veio da Etapa 3.5 (NF **LANÇADA** no ERP, não pendente), zerar `marca.lojas[loja].transito = 0` e `produto[loja].transito = 0` para todos produtos da marca. Se vier só de NFe pendente (Etapa 3.6), NÃO zerar — mercadoria ainda em trânsito. Logar no relatório quantas (marca, loja) foram zeradas. ⚠️ **PISO PENDENTE `transito_pend` (bug corrigido 2026-06-25, caso Lizze L4 NFe 109390).** A 3.7 zerava o trânsito da marca×loja inteira quando havia QUALQUER lançamento recente — apagando junto o trânsito de uma NFe **diferente, ainda pendente** (não lançada) da mesma marca/loja. Sintoma: gerente diz "a NF X do fornecedor está em trânsito mas não aparece na sugestão". **Fix:** `add_transito_produto(..., pend_floor=True)` acumula um campo `produto[loja].transito_pend` SOMENTE quando a NFe ainda não foi lançada (dedup por nº de doc: `docs_lancados = {n['doc'] sem zeros à esquerda}`). A 3.7 agora zera para o **piso** `transito_pend` (não para 0) via helper `_zera_transito(mk,loja)`. Resultado: trânsito de NF já-chegada some, mas o de NF ainda-pendente sobrevive. Antes do fix o total de trânsito do sistema estava artificialmente em ~66 un (over-zeramento sistêmico); depois voltou a ~3000 un (pendentes reais: Santa Clara/L1, Risque/L3, Marco Boni/L5, Beauty Color/L3 etc). Constante implícita: `nf_ainda_pendente = NumeroNFe não está em docs_lancados`.
- **NFes pendentes em Compras Mês a Mês (regra usuário 2026-05-15):** Etapa 3.6 contribui pra `compras_mensais_rs` rateado por `ValorBruto` dos itens, mês = `DataEmissao`. Trânsito per-produto: match fuzzy ≥50% tokens contra `marca.produtos[]`; órfãos viram entradas novas com `codigo='NF-<CProd>'`, `_origem='NFe pendente'`. Dashboard destaca com badge NOVO amarelo (produto novo ou código alterado pelo fabricante). ⚠️ `NaturezaOperacao` da API chega TRUNCADA em 10 chars — sempre combinar filtro de natureza com filtro de CFOP de exclusão (5152/6152 transferência, 591x/691x remessa/bonificação/amostra/consignação, 5201-5210/6201-6210 e 5410-5413/6410-6413 devoluções, 1411/2411/3411 devolução de venda).
- **Etapa 5.4 (limpeza, obrigatória):** fechar via `tabs_close_mcp` todas as abas abertas pela skill (relatorio_compra_venda_saldo, relatorio_notas, entrada_nfe). Manter SÓ a tab principal `v4/home/index.asp`. Não fechar abas do usuário.
- **Etapa 5.5:** `git add` + `commit` + `push origin main` em `/Users/elkgomes/Desktop/claude/compras` pra refletir no GitHub Pages.
- Saída em peças (não R$).

